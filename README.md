# CLEDIAN
AI trợ lý phát hiện nội dung lừa đảo tiếng Việt sử dụng Multinomial Naive Bayes.

## Chạy nhanh
```bash
pip install -r requirements.txt
uvicorn api.main:app --reload
```
Mở trình duyệt: http://127.0.0.1:8000
